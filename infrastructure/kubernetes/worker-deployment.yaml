apiVersion: v1
kind: ServiceAccount
metadata:
  name: fresheats-worker
  namespace: fresheats
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/fresheats-worker-irsa
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fresheats-worker
  namespace: fresheats
  labels:
    app: fresheats-worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fresheats-worker
  template:
    metadata:
      labels:
        app: fresheats-worker
    spec:
      serviceAccountName: fresheats-worker
      containers:
        - name: worker
          image: ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/fresheats-worker:latest
          command: ["python", "-m", "app.sqs_consumer"]
          ports:
            - containerPort: 8001
          envFrom:
            - configMapRef:
                name: fresheats-config
            - secretRef:
                name: fresheats-secrets
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 3Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: fresheats-worker
  namespace: fresheats
spec:
  selector:
    app: fresheats-worker
  ports:
    - port: 8001
      targetPort: 8001
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: fresheats-worker-hpa
  namespace: fresheats
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: fresheats-worker
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 65
