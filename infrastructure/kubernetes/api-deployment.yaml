apiVersion: v1
kind: ServiceAccount
metadata:
  name: plate-api
  namespace: plate
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/plate-api-irsa
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: plate-api
  namespace: plate
  labels:
    app: plate-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: plate-api
  template:
    metadata:
      labels:
        app: plate-api
    spec:
      serviceAccountName: plate-api
      containers:
        - name: api
          image: ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/plate-api:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: plate-config
            - secretRef:
                name: plate-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: plate-api
  namespace: plate
spec:
  selector:
    app: plate-api
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: plate-api-hpa
  namespace: plate
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: plate-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
