apiVersion: apps/v1
kind: Deployment
metadata:
  name: fresheats-admin
  namespace: fresheats
  labels:
    app: fresheats-admin
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fresheats-admin
  template:
    metadata:
      labels:
        app: fresheats-admin
    spec:
      containers:
        - name: admin
          image: ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/fresheats-admin:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: fresheats-config
            - secretRef:
                name: fresheats-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: fresheats-admin
  namespace: fresheats
spec:
  selector:
    app: fresheats-admin
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
